{
  "name": "learn-ddd",
  "module": "index.ts",
  "type": "module",
  "scripts": {
    "migrate": "bunx prisma migrate dev --name migration",
    "generate": "bunx prisma generate"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "@prisma/client": "^6.3.1"
  }
}
